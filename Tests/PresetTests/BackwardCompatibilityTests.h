#pragma once

namespace backward_compat_tests
{
// Run all backward compatibility regression tests.
// Returns the number of failed tests (0 = all passed).
int runAll();
} // namespace backward_compat_tests
