#pragma once
// XOriginate — canonical include for the XOriginate export engine.
// The implementation lives in XPNExporter.h; this header provides the
// expected include path referenced by tests and external consumers.
#include "XPNExporter.h"
